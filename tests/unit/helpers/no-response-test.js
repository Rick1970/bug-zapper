import { noResponse } from 'bug-zapper/helpers/no-response';
import { module, test } from 'qunit';

module('Unit | Helper | no response');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noResponse([42]);
  assert.ok(result);
});
